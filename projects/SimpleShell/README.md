# Simple Shell

A custom implementation of a Unix shell, capable of parsing command lines and executing programs.

## Features
- Process creation (fork/exec)
- Signal handling
- Built-in commands (cd, exit)

## Build & Run

```bash
gcc -o simple_shell main.c simple_shell.c
./simple_shell
```
